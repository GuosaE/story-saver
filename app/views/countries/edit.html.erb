<div align="center"><h2 class="basic-header">Edit Country</h2></div>

<%= render 'form', country: @country %>

<br>
<nav class="basic-link">
<%= link_to 'Show', [@country.region, @country] %> 
<%= link_to 'Back', region_countries_path(@country.region) %></nav>