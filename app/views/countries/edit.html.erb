<div align="center"><h1>Edit Country</h1></div>

<%= render 'form', country: @country %>

<%= link_to 'Show', [@country.region, @country] %> 
<%= link_to 'Back', region_countries_path(@country.region) %>