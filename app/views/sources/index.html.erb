<br>
<div align="center">
<table>
  <thead>
    <tr>
      <th>Source</th>
      <br>
      <th>Article Title</th>
    </tr>
  </thead>

  <tbody>
    <% @sources.ordered_by_name.each do |source| %>
      <tr>
      <td style="text-align:center"><%= source.name %></td>
      <td style="text-align:center">
        <% @articles.each do |article| %>
          <% if article.source_id == source.id %>
              <%= article.title %>
          <% end %>
        <% end %>
      </td>
      </tr>
    <% end %>
  </tbody>

    </tr>
  </thead>
</table>

<br>

<br>
<br>
<nav class="basic-link"><%= link_to 'Back to Home', home_index_path %></nav></div>